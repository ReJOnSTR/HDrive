<ContentDialog
    x:Class="HDriveWin.Views.LocalShareDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Hızlı Kablosuz Paylaşım (QR Kod)"
    CloseButtonText="Kapat"
    DefaultButton="Close">

    <StackPanel Spacing="16" Width="400" Margin="0,4,0,0">
        <!-- QR Kod Kartı -->
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="16"
                HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" Spacing="12">
                <Border Background="White" CornerRadius="12" Padding="12" Width="220" Height="220" HorizontalAlignment="Center">
                    <Image x:Name="QrImage" Width="196" Height="196" Stretch="Uniform" />
                </Border>
                <TextBlock Text="Telefonunuzun kamerasıyla QR kodu okutun"
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Bağlantı Adresi ve Butonlar -->
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12,10">
            <Grid ColumnDefinitions="*,Auto" VerticalAlignment="Center">
                <StackPanel VerticalAlignment="Center" Spacing="2">
                    <TextBlock Text="Yerel Web Adresi:" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock x:Name="UrlTextBlock" FontFamily="Consolas" FontWeight="SemiBold" FontSize="13" />
                </StackPanel>
                <Button Grid.Column="1" Click="CopyButton_Click" ToolTipService.ToolTip="Adresi Kopyala">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8C8;" FontSize="12" />
                        <TextBlock Text="Kopyala" FontSize="12" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Ek Eylemler -->
        <Grid ColumnDefinitions="*,*">
            <Button Grid.Column="0" Margin="0,0,4,0" HorizontalAlignment="Stretch" Click="OpenBrowser_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE774;" FontSize="12" />
                    <TextBlock Text="Tarayıcıda Aç" FontSize="12" />
                </StackPanel>
            </Button>
            <Button Grid.Column="1" Margin="4,0,0,0" HorizontalAlignment="Stretch" Click="OpenFolder_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xED43;" FontSize="12" />
                    <TextBlock Text="Klasörü Aç" FontSize="12" />
                </StackPanel>
            </Button>
        </Grid>
    </StackPanel>
</ContentDialog>
