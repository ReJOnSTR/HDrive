<ContentDialog
    x:Class="HDriveWin.Views.QuickLookDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Hızlı Bakış"
    PrimaryButtonText="Uygulamada Aç"
    CloseButtonText="Kapat"
    DefaultButton="Close">

    <Grid Width="520" Height="400" RowDefinitions="*,Auto" Margin="0,8,0,0">
        <!-- Önizleme Ana Alanı -->
        <Border Grid.Row="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <Grid>
                <!-- Resim Önizleme -->
                <Image x:Name="PreviewImage" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />

                <!-- Metin / Kod Önizleme -->
                <ScrollViewer x:Name="PreviewTextScroll" Visibility="Collapsed" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="PreviewTextContent" FontFamily="Consolas, Cascadia Code, Courier New" FontSize="12" LineHeight="18" TextWrapping="Wrap" />
                </ScrollViewer>

                <!-- Jenerik / Büyük İkonlu Önizleme -->
                <StackPanel x:Name="PreviewGenericStack" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16" Visibility="Visible">
                    <Border Width="80" Height="80" CornerRadius="12" Background="{ThemeResource LayerFillColorDefaultBrush}" HorizontalAlignment="Center">
                        <Grid>
                            <Image x:Name="PreviewGenericIconImage" Width="64" Height="64" Stretch="Uniform" Visibility="Collapsed" />
                            <FontIcon x:Name="PreviewGenericGlyph" FontSize="44" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <StackPanel Spacing="4" HorizontalAlignment="Center">
                        <TextBlock x:Name="PreviewFileName" FontSize="16" FontWeight="SemiBold" TextAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="440" />
                        <TextBlock x:Name="PreviewFileType" FontSize="12.5" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextAlignment="Center" />
                    </StackPanel>
                </StackPanel>

                <!-- Yükleniyor Göstergesi -->
                <ProgressRing x:Name="PreviewLoadingRing" Width="36" Height="36" IsActive="False" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Alt Bilgi Şeridi -->
        <Border Grid.Row="1" Margin="0,12,0,0"
                Background="{ThemeResource LayerFillColorAltBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,8">
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                    <TextBlock x:Name="PreviewFileSize" FontSize="11.5" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock x:Name="PreviewFileDate" FontSize="11.5" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <TextBlock Grid.Column="1" Text="Boşluk Tuşu (Space)" FontSize="10.5" Foreground="{ThemeResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</ContentDialog>
